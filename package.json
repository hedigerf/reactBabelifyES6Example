{
  "name": "foo",
  "version": "0.0.0",
  "dependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "babel-preset-es2015": "latest",
    "babel-preset-react": "latest"
  },
  "devDependencies": {
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  },
  "scripts": {
    "build-js": "browserify App.jsx -o bundle.js -d -t [ babelify --presets [ es2015 react ] ]"
  }
}
